# /etc/ldap/ldap.conf — LDAP client defaults for lab environment
# Used by the ldap-client container in Lab 01-02

BASE    dc=lab,dc=localhost
URI     ldap://ipa.lab.localhost

# Disable cert verification in lab (self-signed cert)
TLS_REQCERT never

# Timeouts
TIMELIMIT 15
TIMEOUT   20
